is_inactive = $('#<%= j @todo.id %> .img-check').hasClass("inactive");
if (is_inactive){
  $('#<%= j @todo.id %> .img-check').removeClass("inactive").addClass("check");
}
else{
    $('#<%= j @todo.id %> .img-check').addClass("inactive");
}

// reset_arrow();
