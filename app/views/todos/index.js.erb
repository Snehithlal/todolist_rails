$('.apple_pagination').html(`<%=will_paginate @list_todos %>`)

$('.todolist').append(`
  <% if !@list_todos.empty? %>
  <% @list_todos.each do |todo| %>
  <%= render 'todolist', todo: todo %>
  <% end %>
  <% end %>
`)
$('.pagination a').attr('data-remote', 'true')
reset_arrow();
